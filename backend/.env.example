# Backend database connection used by the FastAPI service and seed scripts.
# Replace YOUR_REAL_PASSWORD with the actual Supabase Postgres password.
SUPABASE_DB_URL=postgresql://postgres:YOUR_REAL_PASSWORD@db.your-project-ref.supabase.co:5432/postgres

# Optional backend tuning.
DB_CONNECT_TIMEOUT=5
DB_PROBE_CACHE_TTL_SECONDS=10

# Optional CORS allow-list. Use * for local development or a comma-separated list of origins.
ALLOWED_ORIGINS=*
